<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('./head'); %>
  </head>
  <body class="container">
    <header><%- include('./header'); %></header>

    <main>
      <div class="row">
        <ul>
          <li>
            <strong>Item Id --> <%= item.id %></strong> <br />
            <strong>Item User --> <%= item.user_id %></strong><br />
            <strong>Item Name --> <%= item.name %></strong><br />
            <strong>Item category --> <%= item.category_id %></strong><br />
            <strong>Item Images --> <%= item.image %></strong><br />
            <strong>Item Brand --> <%= item.brand_id %></strong><br />
            <strong>Item Price --> <%= item.price %></strong><br />
            <strong>Item Link --> <%= item.link %></strong><br />
            representing Afilio, born <%= item.created_at %>
          </li>
        </ul>
      </div>
    </main>

    <footer><%- include('./footer'); %></footer>
  </body>
</html>
